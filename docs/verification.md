# 验证记录与边界

日期：2026-09-13。此文件是脱敏摘要，不包含会话 ID、个人目录或原始日志。

## 本轮通用性审查后实测

| 检查 | 结果 | 能证明什么 |
| --- | --- | --- |
| `python3 -m unittest discover -v` | 47 项通过 | 规则、协议字段、子进程传输和模拟安装/卸载行为 |
| 安装器路径含空格、启动脚本执行 | 通过 | 在临时目录运行生成的管理命令；不会改动真实 LaunchServices |
| 缺失 CLI、非 macOS 安装 | 通过 | 在写启动环境之前停止 |
| `python3 live_probe.py --live --model gpt-6-astra` | 通过 | 新控制器经原版服务端完成两轮真实推理：medium → low、low → high；服务端报告的实际档位一致 |
| 双向大数据与超大帧 | 通过 | 同时 2 MB 输入/输出，以及 17 MB 单帧和无换行尾帧完整透传 |
| 安装故障回滚 | 通过 | 模拟 bootstrap / setenv 失败后恢复文件、环境和原登录服务 |
| 当前本机协议 schema | 已核对 | turn/start、thread/settings/update、模型目录及计划模式字段与本机 CLI 生成结果对应 |
| 原版 CLI | `0.154.0-alpha.6.2` | 本机此次验证版本 |
| 文档、源码公开扫描 | 通过 | 发布文件未包含本机个人路径、凭据、会话 ID 和运行状态文件 |

本次没有重新安装或重启用户桌面应用。安装器的 LaunchServices 操作在测试中使用模拟实现；新控制器已在真实 App Server 中验证；新版安装器和桌面重启接入尚未在其他人的真实桌面上验证。

## 原始实现已有证据

首次开源整理时核对了原项目保留的验证记录；以下哈希仅代表原始版本，不代表本轮修复后的源码：

- 两轮真实服务端推理分别确认 `low` 和 `high`，且轮次完成。
- 原项目桌面重启后的单轮请求经过包装程序，服务端报告 `medium`，请求被接受。
- 控制器 SHA-256：`f605d9faece0cf05c23f2cb236c048dff3e935be7382bdf177ce2bc19a072e72`。

以上是原项目的历史本机记录。当前版本真实服务端验证见上表；新版桌面重启、其他版本、其他账号及所有档位仍需分别验证。

## 未验证

- token/费用节省幅度、回答质量、规则分类准确率。
- 所有档位在桌面内的实际效果和 UI 选择器显示。
- 不同桌面版本、其他用户账号、非 macOS 的桌面接入。
- 更新、异常关机、磁盘故障等情况下的长期稳定性。

GitHub Actions 覆盖 Ubuntu/macOS 与 Python 3.9、3.12、3.13，运行离线测试，不执行真实模型请求，也不安装桌面启动接入；实际 CI 结果以仓库 Actions 页面为准。
